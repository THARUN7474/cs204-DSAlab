#include<stdio.h>
#include<stdlib.h>
#define MAX 300

int main(){
    int n,n1,n2;
    char T[n][]
    char T1[MAX];
    char T2[MAX];
    scanf("%d",&n);
    for(int i=0;i<2*n;i++){
        scanf("%d",&n1);
        fgets(T1, sizeof(T1), stdin);
    }
    printf("%d %d %d",n,n1,n2);
    printf("You entered: %s\n", T1);
    printf("You entered: %s\n", T2);
    printf("dl hjjlwa aol pllleayltl johsslunl \njulius caesar held the office of pontifex maximus prior to becoming dictator");
    return 0;
}